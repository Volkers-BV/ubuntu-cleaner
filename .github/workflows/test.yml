name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck
        run: sudo apt-get install -y shellcheck

      - name: Run ShellCheck
        run: shellcheck logcleaner.sh

  test:
    name: Ubuntu ${{ matrix.ubuntu }}
    needs: shellcheck
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ubuntu: ["18.04", "20.04", "22.04", "24.04"]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Build test image
        env:
          DOCKER_BUILDKIT: "1"
        run: |
          docker build \
            --build-arg UBUNTU_VERSION=${{ matrix.ubuntu }} \
            --cache-from type=gha,scope=${{ matrix.ubuntu }} \
            --cache-to type=gha,scope=${{ matrix.ubuntu }},mode=max \
            -t logcleaner-test:${{ matrix.ubuntu }} \
            -f docker/Dockerfile.base .

      - name: Run unit tests
        run: |
          docker run --rm \
            logcleaner-test:${{ matrix.ubuntu }} \
            bats --recursive tests/unit/

      - name: Run integration tests
        run: |
          docker run --rm --privileged \
            logcleaner-test:${{ matrix.ubuntu }} \
            bats --recursive tests/integration/
